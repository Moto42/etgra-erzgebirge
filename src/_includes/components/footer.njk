<footer class="flex w-full bg-txinfomedia-bg-standout p-4 text-sm justify-between flex-wrap">
    <div class="flex flex-col justify-center items-start">
        <div class="">{{contactinfo.phone}}</div>
        <div class="">{{contactinfo.addr1}}</div>
        <div class="">{{contactinfo.addr2}}</div>
    </div>
    <div class="flex items-center justify-center"><img src="{{headerfooter.logo}}" alt=""></div>
    <div class="flex items-center">&copy; Texas Infomedia {% thisYear %}</div>
    <div class="w-full flex justify-center mx-3 texts-xs *:text-[rgba(0,0,0,0.7)]">
        {% for item in headerfooter.footerlinks %}
            <a href="{{item.url}}">{{item.text}}</a>
        {% endfor %}
    </div>
</footer>